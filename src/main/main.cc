#include "game.h"
#include "perspectiveutil.h"
#include "geometryutil.h"
#include <cmath>
#include <iostream>

int main() {
	srand(time(NULL));
	Game game;
	game.start();
	// Vector v1;
	// v1.direction = 0;
	// v1.magnitude = 1;
	//
	// Vector v2;
	// v2.direction = M_PI / 2;
	// v2.magnitude = 1;
	//
	// v1 += v2;
	// std::cout << v1;
	// Launcher launcher;
	// launcher
    return 0;
}
